{
  "name": "aws-resources",
  "version": "1.0.0",
  "description": "Pulumi program to deploy AWS resources via Kubernetes Operator",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "start": "pulumi up"
  },
  "dependencies": {
    "@pulumi/pulumi": "^3.88.0",
    "@pulumi/aws": "^6.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
